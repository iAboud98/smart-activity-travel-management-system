<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background"
    android:orientation="vertical"
    android:padding="@dimen/screen_margin">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Registered Users"
        android:textAppearance="@style/TextAppearance.TravelPlanner.Headline" />

    <TextView
        android:id="@+id/users_empty_state"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_large"
        android:gravity="center"
        android:text="No users registered yet."
        android:textAppearance="@style/TextAppearance.TravelPlanner.Body"
        android:visibility="gone" />

    <EditText
        android:id="@+id/admin_users_search_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_medium"
        android:hint="Search by name, email or phone"
        android:inputType="text"
        android:padding="8dp"
        android:background="@color/color_surface" />

    <Button
        android:id="@+id/admin_users_search_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_small"
        android:text="Search" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/users_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="@dimen/space_medium"
        android:clipToPadding="false" />

</LinearLayout>